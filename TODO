Setup:
Add example
Review cmake
binary test_package -> test-package THIS IS BAD. CAN*T DOUBLE CLICK

include(${doctest_SOURCE_DIR}/scripts/cmake/doctest.cmake)

werror (enabled in CI)
warnings

clang-format ?
CI (compile and format)
, minimum version?
CI run cmake instead of conan?
cmake run conan install

build instructions

Application:
logger (avoid static or improve testability with static)
error handling: catch exceptions. throw instead of abort? make sure proper glfw termination with exceptions
Title?
